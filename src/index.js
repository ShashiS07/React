import React from 'react';
import ReactDOM from 'react-dom/client';
import TextArea from './AssignmentTextArea';
const root = ReactDOM.createRoot(document.getElementById('root'));

  root.render(
    <React.StrictMode>
      <TextArea/>
    </React.StrictMode>
  );


